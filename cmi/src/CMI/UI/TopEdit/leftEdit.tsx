import React from 'react'
import { CMIHooksType } from '../../hooks'

type Props = {
    CMIHooks:CMIHooksType
}

export function LeftEdit({CMIHooks}: Props) {
  return (
    <div>LeftEdit</div>
  )
}