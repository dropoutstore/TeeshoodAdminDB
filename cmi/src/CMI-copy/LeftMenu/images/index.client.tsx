import React from 'react';
import { LeftMenuButton } from '../../CMIComponents/LeftMenuButton.client';

export default function Images() {
  return <LeftMenuButton onClick={() => void 0}>Image</LeftMenuButton>;
}
